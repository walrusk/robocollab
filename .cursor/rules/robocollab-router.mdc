---
description: RoboCollab sticky mode router
alwaysApply: true
---

Follow `AGENTS.md`. COLLAB instructions are inline there. Do not preemptively load `.ai/modes/*.md` or `.ai/workflows/*.md` — read mode files only for DEV or FOLLOWUP, and read workflow files only when the active task requires them.
