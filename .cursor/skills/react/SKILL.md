---
name: react
description: >
  Use when editing React or React Native components.
---

# React

For React components, use `type Props = {}` for props rather than giving the type a component-specific name.
